# V3.4 更新方案说明

> 依据：2026-08-26 下午至晚间问题讨论与代码核查（含晚间补充需求，对照见第五节）
> 原则：简单优先——只修真问题，不做时间线可视化、组织架构版本管理等重设计

---

## 一、问题清单（讨论确认）

### 1. 数据安全：无自动备份
- 数据存于本地 IndexedDB（安装版在 C 盘用户目录，绿色版在 exe 同级），重装系统/误删文件夹即全量丢失
- 现仅有手动 JSON 导出，依赖用户自觉

### 2. 人员状态时点判定：5 个漏洞
| # | 漏洞 | 后果 |
|---|---|---|
| 1 | 状态历史只能追加、无法修正 | 误标"借调"再改回在职后，该窗口内的会议**永远无法录入此人**（用户实际遇到的问题） |
| 2 | 搜索添加静默降级为临时人员 | 非该日期在职的正式人员搜不到 → 建临时人员，考勤统计为 0，用户无感知 |
| 3 | 智能解析与手动添加口径不一致 | 粘贴接龙文本可导入非在职正式成员，手动添加却不行 |
| 4 | `appendStatusChange` 去重基准错误 | 只比较数组末位，回溯补录早于末位的变更时判断失真 |
| 5 | 删除重建人员孤儿化历史会议 | 新 memberId 与旧会议 participants 悬空，历史考勤悄然缩水 |

### 3. 批量导入 + 回填历史会议：时间线失真
- 导入模板无状态列，全员强制"在职"，初始记录日期 = 导入当天（非入职日）
- `isActiveAt` 对"会议日期早于首条记录"一律默认在职 → 导入前已调离/借调的人，历史会议全部误判为在职

### 4. 党小组设置调整：3 个缺口
| # | 缺口 | 影响 | 处置 |
|---|---|---|---|
| 1 | 人员表单党小组只能选预设 5 个组 | 大调整时新组名**无法录入** | 本次修 |
| 2 | 人员无党小组历史 | 仅影响回填旧会议时的批量选人，历史数据安全（会议存快照） | 不修（搜索添加兜底） |
| 3 | 小组更名导致统计割裂 | 数据不丢，年度导出拆成两张子表 | 暂不纳入（后续版本视需要再加更名替换小工具） |

### 5. 部门/部室变更：口径矛盾（真实漏洞）
- 会议记录的党小组是**快照**，但 participants 不存部门 → 看板"部门出勤率"与导出报表用**当前部门回填全部历史**
- 张三年中换部门：两个部门的历史出勤曲线都被改写；同一张报表党小组用旧值、部门用新值，口径混用无提示

### 6. 历史数据安全性结论（无需修复的部分）
- 人员换组/小组解散合并：会议存的是组名快照，**历史数据不受影响**
- 考勤统计（`countActiveAttendance`）为读取时实时计算，无缓存，任何数据修正后自动全局重算

---

## 二、更新方案（10 项功能 + 1 项规则修正）

### 功能 1：定时自动备份（在现有手动备份基础上叠加，不新建备份机制）
- **实现方式**：复用现有手动备份的导出函数与 JSON 格式（`backup.ts`），仅叠加"调度器 + 目标路径 + 滚动清理"三件事；设置页配置区放在现有"数据备份"区块内
- **UI**：目标路径选择 + 频率（每30分钟/每1小时/每天8点/仅启动时）+ 保留份数（默认 30）+ 格式（JSON / JSON+Excel）
- **文件名**：`自动备份_党建工作台账_YYYYMMDD_HHMMSS.json`（与手动备份同格式，可导入恢复）
- **格式**：JSON 与 Excel **均可整库恢复**；选"JSON+Excel"时同时生成两份文件：
  - **JSON**：整库无损备份（同手动备份格式）
  - **Excel**：人读版台账（复用 `exportExcel.ts` 现有导出：会议明细 + 分类子表 + 人员 + 谈心谈话，与组长填报模板 8c 同字段结构），**另附一个隐藏 Sheet `BACKUP_JSON`**——整库 JSON 按行分块存储（规避单元格 32767 字符上限），供恢复读取
- **恢复入口升级**：数据恢复同时接受 `.json` 与 `.xlsx`——xlsx 优先读 `BACKUP_JSON` 隐藏表，拼块还原整库 JSON 后**与 JSON 恢复走同一代码路径，无损恢复**（含状态历史、变更历史、参会明细快照）；无该隐藏表的 xlsx（组长手工表、台账导出）提示"非备份文件，请走数据融合入口"
- **默认路径**：绿色版 = exe 同级 `backups\`；安装版 = `文档\党建工作台账备份\`
- **安全写入**：先写临时文件再原子重命名
- **滚动清理**：超保留份数自动删最旧
- **防重复融合**：自动融合扫描排除"自动备份_"前缀
- 改动：主进程 IPC 写文件 + 设置页 UI + 调度器（约 4 个文件）

### 功能 2：人员变更历史管理（状态可编辑 + 信息变更自动留痕）
**2a. 状态历史可编辑表格（核心修复，极简版）**
- **UI**：编辑人员弹窗底部新增 antd Table——列：变更日期（行内 DatePicker）/ 状态（行内 Select）/ 删除按钮；底部"+ 添加记录"
- **校验仅两条**：日期不可重复、不可晚于今天
- **提交**：整包按日期排序保存 `statusHistory`，替代现有"仅追加"逻辑
- **无**时间线图、无删除确认弹窗、无补录表单、无行权限区分（全部删空时回退按当前状态判定兜底）
- 效果：误标窗口删一行即消除；导入日初始记录改日期即校准入职锚点

**2b. 信息变更自动留痕（同一入口统一查看）**
- **数据结构**：member 新增可选字段 `changeHistory?: Array<{date, field, oldValue, newValue}>`，旧数据无此字段完全兼容
- **自动记录**：编辑保存时 diff 字段（党小组/部门/部室/支委职务/组长/电话/姓名/状态），有变化即自动追加一条，**无需用户手动填写**
- **UI**：与 2a 同一区块合并展示为"变更历史"表格，增加"类型"列——状态行可编辑（同 2a），信息变更行**只读**（展示 `字段：旧值 → 新值`）
- **附带收益**：低成本补上党小组/部门的历史轨迹（只读查询版）——回填旧党小组会时可查"某人当时在哪个组"，替代此前搁置的"党小组历史时间线"方案
- **不做**：信息变更不参与任何统计判定（统计仍按功能 6 的会议时点快照），仅作审计与查询
- 改动：MemberForm.tsx（~70 行）+ Members.tsx（diff 追加 ~20 行）+ types（~5 行）

**2c. 删除人员（保留信息）时状态与日期可选**
- **现状缺陷**：单个删除与批量删除的"保留信息"分支均写死 `离职 + 今天的日期`——实际调离/借调/离职时间与今天不符时无法选择，且离职原因被强制归一
- **UI**：确认弹窗升级为小表单——状态 Select（调离/借调/离职）+ 变更日期 DatePicker（默认今天，可回填实际日期）
- **批量删除**：弹窗中选择一次状态与日期，统一应用到所有被标记保留的人员（不逐人弹窗，保持批量操作效率）
- **联动**：所选状态与日期直接写入 `statusHistory`，与 2a 可编辑表格无缝衔接（删错了可在编辑弹窗修正）
- 改动：Members.tsx（~30 行）

### 功能 3：搜索添加的明确提示（漏洞 2 修复）
- 搜索不到时判断"人员库存在但非该日期在职"，提示原因 + "仍强制添加为正式成员"确认入口
- 不再静默创建临时人员

### 功能 4：批量导入模板增强（可选列，不填维持现状）
- 新增 3 列：`入职日期`（初始记录日期）、`当前状态`（可导入即调离/借调）、`状态变更日期`
- 导入时构建完整时间线；模板留空则行为与现在完全一致

### 功能 5：党小组下拉自由输入（缺口 1 修复，约 5 行）
- 人员表单党小组 Select 支持自定义录入，选项 = 预设 5 组 ∪ 现有人员已用组名，与会议表单口径对齐

### 功能 6：参会记录部门快照（部门口径漏洞修复）
- 保存会议时在每条 participant 顺带存当时 `department`/`title`（约 10 行）
- 统计与导出优先读快照，无快照的旧数据回退当前值（向后兼容，不迁移）
- 看板部门出勤率图标题注明"按会议时点部门统计"

### 功能 7：导入查重（重复人员拦截）
- **批量导入**：导入时按姓名比对本机人员库，对同名行给出处理选择——**跳过（默认）/ 用导入信息更新该人员 / 仍新建**；结果汇总提示（新增 X 人、更新 Y 人、跳过 Z 人）
- **手动新增**：补齐同名拦截——同名在职人员也提示"已存在同名人员，是否仍新建？"（复用现有离职恢复弹窗交互模式）
- **备份融合**：同名提示与 id 去重维持现状；字段级冲突处理见功能 8a
- 改动：Members.tsx（~25 行）
- 背景漏洞：当前批量导入无任何查重，同名直接新增 → 同一人变两条记录，考勤拆分、看板人数虚增、`matchMember` 只命中第一条形成僵尸数据

### 功能 8：融合冲突处理（人员字段级融合 + 重复提示）
**8a. 人员信息字段级融合（本机非空白为准，冲突弹窗确认）**
- 融合粒度从"整条记录保留本机"细化为**字段级**：
  - 本机字段**非空白**、与备份不同 → **以本机为准**，列入冲突提醒
  - 本机字段**空白**、备份有值 → **自动补齐**（报告提示"自动补充空白字段 X 处"，无需确认）
  - 两侧一致 → 无动作
- **冲突提醒弹窗**（点击执行融合时若存在冲突则先弹出）：表格列出 人员 / 字段 / 本机值 / 备份值 / 采用（单选，默认本机）
  - **批量确认**："全部保留本机 / 全部采用备份"一键设置
  - **逐条确认**：每行独立切换采用哪一侧
- 参与字段：姓名、部室、部门/支部、党小组、支委职务、党小组组长、联系电话
- **状态与状态历史不参与字段融合**（两条时间线自动合并语义复杂、易错）；状态不一致仅在报告提示，事后由编辑弹窗（功能 2a）处理
- 改动：mergeData.ts（字段 diff + 应用，~50 行）+ Settings.tsx（冲突弹窗，~60 行）

**8b. 会议/谈话疑似重复提示（仅提示，不拦截）**
- id 不同但"日期 + 主题"相同的会议：正常并入，报告中列出"疑似重复会议 N 条（建议手动清理）"
- 谈话比照处理（日期 + 参与双方）
- 改动：mergeData.ts（~15 行）+ 报告文案

- 背景：原"整条保留本机"会静默丢弃组长侧的电话/党小组等更新；字段级融合 + 冲突弹窗使差异可见、可控

**8c. Excel 数据融合（融合入口支持 Excel 格式；组长手工表导入）**
- **场景**：数据融合入口同时接受 `.json` 备份与 `.xlsx` 文件——组长不使用本应用，按模板手工填表交给管理员，管理员在"数据融合"入口直接导入；自动备份生成的 Excel（功能 1）同为该格式，亦可作融合源
- **模板字段与现有会议台账导出完全一致**（`exportExcel.ts` `DETAIL_HEADERS` 17 列）：序号 / 会议名称 / 会议日期 / 会议时间 / 会议类型 / 所属党小组 / 会议地点 / 主持人 / 记录人 / 会议议题 / 会议决议结论 / 应到人数 / 实到人数 / 请假人数 / 缺席人数 / 出勤率 / 参会人员名单——**模板即"台账导出的空表版"**，组长看到的列与应用录入字段一一对应，杜绝字段歧义
- **填写说明**（模板表头上方嵌说明行）：日期格式 YYYY-MM-DD；会议类型填可选值（支委会/党员大会/主题党日/党课/组织生活会/党小组会等）；参会人员名单按导出同款分栏格式 `参会：张三、李四` / `请假：王五(事假)` / `缺席：赵六`（列席标记 `(列席)`）
- **模板获取**：设置页新增"下载组长填报模板"按钮生成空表（含说明行与示例行 1 条）
- **人员匹配**：参会姓名与本机人员管理按姓名匹配（与会议表单 `matchMember` 同口径），**不自动新建人员**
- **导入前校验报告**（准确性保障，逐项列出后由用户确认或修正重导）：
  - 本机匹配不到的姓名 → 列出并标记"未匹配"，可选择按临时人员并入或取消该人
  - 日期格式非法、类型不在可选值、名单分栏格式错误 → 列出**行号与原因**，整表拦截要求修正
  - 应到/实到/请假/缺席四列与名单分栏人数不一致 → 以**名单明细为准**，差异行列出提示
  - 与本机"日期+名称"相同的疑似重复 → 按 8b 口径提示
- **解析器复用**：`importGroupExcel.ts` 一个解析器通吃三种来源（组长手工表 / 自动备份 Excel / 日台账导出的回导核对）
- 改动：Settings.tsx（入口/模板下载/校验报告 UI）+ 新增 `src/utils/importGroupExcel.ts`（解析+校验，~120 行）

### 功能 10：数据看板口径调整
- **文案**：统计卡"在职人员总数"改为"**在职党员总数**"（同步排查导出报表、其他页面的同类表述）
- **出勤率排行**：仅显示**当前在职**人员——调离/借调/离职者不出现在排行中；其状态改回在职后自动重新出现（统计为读取时实时计算，无需迁移）
- **范围限定**：仅调整"党员出勤率排行"列表；部门出勤率图、导出报表口径不变（仍按统计期内活跃人员），避免两套口径误伤
- 改动：Dashboard.tsx（文案 1 行 + 排行过滤 ~3 行）

### 规则修正：早于首条记录的判定（1 行，影响存量数据需知会）
- `isActiveAt`：会议日期早于首条状态记录时，从"默认在职"改为"**沿用首条记录状态**"
- 首条为在职的人（绝大多数）行为不变；首条为调离者，调离前的会议不再误计在职
- ⚠️ 副作用：当前非在职且未维护过历史的人，历史考勤数字会变小（更准确），更新说明中需提示并引导校准初始日期

---

## 三、明确不做的事（维护性考量）

| 项 | 理由 |
|---|---|
| 状态时间线可视化（SVG） | 排序表格已足够表达 |
| 删除影响预览弹窗 | 保存即实时重算，无需预览 |
| 人员党小组历史时间线（参与统计判定的完整版） | 已由功能 2b 的只读留痕低成本覆盖查询需求；统计判定仍走功能 6 会议时点快照，不做双轨 |
| 组织架构版本管理 | 过度设计 |
| 删除人员按姓名重联历史会议（漏洞 5） | 场景边缘，暂观察；恢复备份可兜底 |
| 智能解析口径统一（漏洞 3） | 与功能 3 合并考虑，若功能 3 落地则解析侧同样走确认提示（实施时顺带，不单独设计） |
| 融合时状态/状态历史自动合并 | 两条时间线自动合并语义复杂、易错；仅提示不一致，由功能 2a 编辑弹窗人工处理 |
| 疑似重复会议自动跳过/自动合并 | 同日期同主题可能是真实的两场会，自动判断有误杀风险——只提示，用户手动清理 |

---

## 四、实施与验收要点

**涉及文件**：`electron/main.cjs`、`src/pages/Settings.tsx`、`src/components/MemberForm.tsx`、`src/pages/Members.tsx`、`src/components/MeetingForm.tsx`、`src/utils/mergeData.ts`、`src/utils/importGroupExcel.ts`（新增）、`src/utils/memberStatus.ts`、`src/utils/exportExcel.ts`、`src/pages/Dashboard.tsx`、`src/types/index.ts`、`package.json`（版本号）

**版本与兼容性**：
- `package.json` version → `3.4.0`，应用内关于页/标题栏版本号同步
- **备份恢复向后兼容**：V3.2/V3.3 导出的 JSON 备份在 V3.4 可正常恢复与融合（恢复路径已有 schemaVersion 迁移机制，V3.4 新增字段均为可选，旧备份缺字段走默认值）
- **数据库升级不破坏旧数据**：IndexedDB schema 版本递增走 `db/index.ts` 现有 upgrade 链路，新索引/新字段对旧记录惰性兼容
- 后续迭代（V3.5+）延续两条约定：新字段一律可选 + 备份含 schemaVersion 迁移，保证老备份永远可导入新版本

**测试场景**（在现有 `scripts/v33-test.ts` 基础上扩展）：
1. 误标借调→删除该行→原无法录入的会议可录入且考勤恢复
2. 批量导入含入职日期/调离状态列→回填历史会议判定正确
3. 自动备份按频率生成、滚动清理生效、不被自动融合重复导入；JSON+Excel 模式两文件同时生成且 Excel 三表齐全可打开；**从 Excel 备份整库恢复成功**（读 BACKUP_JSON 隐藏表，状态历史/变更历史/参会明细完整还原，与 JSON 恢复结果一致）；无隐藏表的 xlsx 恢复被拦截并引导至融合入口
4. 人员换部门后，换部门前的会议导出仍显示旧部门（快照）
5. 规则修正后：首条为调离者，调离前会议不计考勤
6. 导入含同名人员的 Excel：三种处理选择（跳过/更新/新建）各自结果正确，汇总数字准确
7. 编辑人员党小组/部门后，变更历史自动出现对应只读记录（旧值→新值）；未变化的字段不产生记录
8. 删除保留信息时选择"借调 + 实际日期"→ 该日期前的会议仍计入考勤、之后不计入；批量删除统一应用所选状态
9. 融合冲突弹窗：本机与备份同 id 人员字段不同 → 批量"全部保留本机"/"全部采用备份"/逐条切换各自正确生效；本机空白字段被自动补齐并计数提示
10. 疑似重复提示：同日期同主题、id 不同的会议并入后报告正确列出，不拦截不误删
11. Excel 融合：融合入口接受 .xlsx；组长模板（17 列台账字段）合法文件正确并入且姓名按本机匹配；未匹配姓名可选择处理；非法日期/类型/名单格式拦截并提示行号；出勤四列与名单不一致以名单为准并提示；自动备份生成的 Excel 可走同一入口融合
12. 看板：非在职人员不出现在出勤率排行，状态改回在职后重新出现；"在职党员总数"文案生效
13. 兼容性：V3.2/V3.3 备份在 V3.4 恢复、融合全流程无异常；旧库升级（无部门快照、无状态历史、无 changeHistory）回归通过

**版本**：V3.4.0；完成后打包安装版 + 绿色版，保留 V3.2/V3.3 安装包

---

## 五、晚间补充需求对照（2026-08-26）

| 补充需求 | 对应方案项 | 状态 |
|---|---|---|
| 看板："在职人员总数"改"在职党员总数" | 功能 10 | 本次新增 |
| 看板：非在职人员不出勤率排行不体现，恢复在职后再出现 | 功能 10 | 本次新增 |
| 人员管理：导入含在职初始日期（非必填） | 功能 4（入职日期列，留空维持现状） | 已覆盖 |
| 会议管理：非在职人员部分会议加不进来 | 功能 2a + 功能 3 + 规则修正 | 已覆盖 |
| 数据管理：定时备份到指定文件夹（JSON/Excel） | 功能 1（在现有手动备份基础上叠加；JSON 与 Excel **均可整库恢复**，Excel 亦可作融合源） | 已覆盖+扩充 |
| 数据管理：融合支持 Excel 版本 | 功能 8c（融合入口接受 .json/.xlsx，同一解析器） | 本次新增 |
| 数据管理：组长手工表格导入+模板+姓名匹配+格式提醒 | 功能 8c（模板 = 现有台账 17 列字段，与应用录入一致） | 本次新增 |
| 数据管理：版本号对应修改 | 第四节"版本与兼容性" | 已覆盖 |
| 备份恢复兼容旧版本、支持后续迭代 | 第四节"版本与兼容性" | 本次新增（明确为验收项） |
