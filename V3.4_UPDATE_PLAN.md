# V3.4 更新方案说明

> 依据：2026-08-26 下午至晚间问题讨论与代码核查
> 原则：简单优先——只修真问题，不做时间线可视化、组织架构版本管理等重设计

---

## 一、问题清单（讨论确认）

### 1. 数据安全：无自动备份
- 数据存于本地 IndexedDB（安装版在 C 盘用户目录，绿色版在 exe 同级），重装系统/误删文件夹即全量丢失
- 现仅有手动 JSON 导出，依赖用户自觉

### 2. 人员状态时点判定：5 个漏洞
| # | 漏洞 | 后果 |
|---|---|---|
| 1 | 状态历史只能追加、无法修正 | 误标"借调"再改回在职后，该窗口内的会议**永远无法录入此人**（用户实际遇到的问题） |
| 2 | 搜索添加静默降级为临时人员 | 非该日期在职的正式人员搜不到 → 建临时人员，考勤统计为 0，用户无感知 |
| 3 | 智能解析与手动添加口径不一致 | 粘贴接龙文本可导入非在职正式成员，手动添加却不行 |
| 4 | `appendStatusChange` 去重基准错误 | 只比较数组末位，回溯补录早于末位的变更时判断失真 |
| 5 | 删除重建人员孤儿化历史会议 | 新 memberId 与旧会议 participants 悬空，历史考勤悄然缩水 |

### 3. 批量导入 + 回填历史会议：时间线失真
- 导入模板无状态列，全员强制"在职"，初始记录日期 = 导入当天（非入职日）
- `isActiveAt` 对"会议日期早于首条记录"一律默认在职 → 导入前已调离/借调的人，历史会议全部误判为在职

### 4. 党小组设置调整：3 个缺口
| # | 缺口 | 影响 | 处置 |
|---|---|---|---|
| 1 | 人员表单党小组只能选预设 5 个组 | 大调整时新组名**无法录入** | 本次修 |
| 2 | 人员无党小组历史 | 仅影响回填旧会议时的批量选人，历史数据安全（会议存快照） | 不修（搜索添加兜底） |
| 3 | 小组更名导致统计割裂 | 数据不丢，年度导出拆成两张子表 | 可选：更名替换小工具 |

### 5. 部门/部室变更：口径矛盾（真实漏洞）
- 会议记录的党小组是**快照**，但 participants 不存部门 → 看板"部门出勤率"与导出报表用**当前部门回填全部历史**
- 张三年中换部门：两个部门的历史出勤曲线都被改写；同一张报表党小组用旧值、部门用新值，口径混用无提示

### 6. 历史数据安全性结论（无需修复的部分）
- 人员换组/小组解散合并：会议存的是组名快照，**历史数据不受影响**
- 考勤统计（`countActiveAttendance`）为读取时实时计算，无缓存，任何数据修正后自动全局重算

---

## 二、更新方案（6 项功能 + 1 项规则修正）

### 功能 1：定时自动备份（新增）
- **UI**：设置页新增"自动备份"区块——目标路径选择 + 频率（每30分钟/每1小时/每天8点/仅启动时）+ 保留份数（默认 30）
- **文件名**：`自动备份_党建工作台账_YYYYMMDD_HHMMSS.json`（与手动备份同格式，可导入恢复）
- **默认路径**：绿色版 = exe 同级 `backups\`；安装版 = `文档\党建工作台账备份\`
- **安全写入**：先写临时文件再原子重命名
- **滚动清理**：超保留份数自动删最旧
- **防重复融合**：自动融合扫描排除"自动备份_"前缀
- 改动：主进程 IPC 写文件 + 设置页 UI + 调度器（约 4 个文件）

### 功能 2：状态历史可编辑表格（核心修复，极简版）
- **UI**：编辑人员弹窗底部新增 antd Table——列：变更日期（行内 DatePicker）/ 状态（行内 Select）/ 删除按钮；底部"+ 添加记录"
- **校验仅两条**：日期不可重复、不可晚于今天
- **提交**：整包按日期排序保存 `statusHistory`，替代现有"仅追加"逻辑
- **无**时间线图、无删除确认弹窗、无补录表单、无行权限区分（全部删空时回退按当前状态判定兜底）
- 改动：MemberForm.tsx（~70 行）+ Members.tsx（~10 行）
- 效果：误标窗口删一行即消除；导入日初始记录改日期即校准入职锚点

### 功能 3：搜索添加的明确提示（漏洞 2 修复）
- 搜索不到时判断"人员库存在但非该日期在职"，提示原因 + "仍强制添加为正式成员"确认入口
- 不再静默创建临时人员

### 功能 4：批量导入模板增强（可选列，不填维持现状）
- 新增 3 列：`入职日期`（初始记录日期）、`当前状态`（可导入即调离/借调）、`状态变更日期`
- 导入时构建完整时间线；模板留空则行为与现在完全一致

### 功能 5：党小组下拉自由输入（缺口 1 修复，约 5 行）
- 人员表单党小组 Select 支持自定义录入，选项 = 预设 5 组 ∪ 现有人员已用组名，与会议表单口径对齐

### 功能 6：参会记录部门快照（部门口径漏洞修复）
- 保存会议时在每条 participant 顺带存当时 `department`/`title`（约 10 行）
- 统计与导出优先读快照，无快照的旧数据回退当前值（向后兼容，不迁移）
- 看板部门出勤率图标题注明"按会议时点部门统计"

### 功能 7：导入查重（重复人员拦截）
- **批量导入**：导入时按姓名比对本机人员库，对同名行给出处理选择——**跳过（默认）/ 用导入信息更新该人员 / 仍新建**；结果汇总提示（新增 X 人、更新 Y 人、跳过 Z 人）
- **手动新增**：补齐同名拦截——同名在职人员也提示"已存在同名人员，是否仍新建？"（复用现有离职恢复弹窗交互模式）
- **备份融合**：维持现状（已有 `sameNameMembers` 同名提示 + 按 id 去重，不改动）
- 改动：Members.tsx（~25 行）
- 背景漏洞：当前批量导入无任何查重，同名直接新增 → 同一人变两条记录，考勤拆分、看板人数虚增、`matchMember` 只命中第一条形成僵尸数据

### 规则修正：早于首条记录的判定（1 行，影响存量数据需知会）
- `isActiveAt`：会议日期早于首条状态记录时，从"默认在职"改为"**沿用首条记录状态**"
- 首条为在职的人（绝大多数）行为不变；首条为调离者，调离前的会议不再误计在职
- ⚠️ 副作用：当前非在职且未维护过历史的人，历史考勤数字会变小（更准确），更新说明中需提示并引导校准初始日期

---

## 三、明确不做的事（维护性考量）

| 项 | 理由 |
|---|---|
| 状态时间线可视化（SVG） | 排序表格已足够表达 |
| 删除影响预览弹窗 | 保存即实时重算，无需预览 |
| 人员党小组历史时间线 | 仅影响回填批量选人，搜索添加已兜底 |
| 组织架构版本管理 | 过度设计 |
| 删除人员按姓名重联历史会议（漏洞 5） | 场景边缘，暂观察；恢复备份可兜底 |
| 智能解析口径统一（漏洞 3） | 与功能 3 合并考虑，若功能 3 落地则解析侧同样走确认提示（实施时顺带，不单独设计） |

---

## 四、实施与验收要点

**涉及文件**：`electron/main.cjs`、`src/pages/Settings.tsx`、`src/components/MemberForm.tsx`、`src/pages/Members.tsx`、`src/components/MeetingForm.tsx`、`src/utils/memberStatus.ts`、`src/utils/exportExcel.ts`、`src/pages/Dashboard.tsx`、`src/types/index.ts`

**测试场景**（在现有 `scripts/v33-test.ts` 基础上扩展）：
1. 误标借调→删除该行→原无法录入的会议可录入且考勤恢复
2. 批量导入含入职日期/调离状态列→回填历史会议判定正确
3. 自动备份按频率生成、滚动清理生效、不被自动融合重复导入
4. 人员换部门后，换部门前的会议导出仍显示旧部门（快照）
5. 规则修正后：首条为调离者，调离前会议不计考勤
6. 导入含同名人员的 Excel：三种处理选择（跳过/更新/新建）各自结果正确，汇总数字准确
7. 旧数据（无部门快照、无状态历史）全流程回归无异常

**版本**：V3.4.0；完成后打包安装版 + 绿色版，保留 V3.2/V3.3 安装包
