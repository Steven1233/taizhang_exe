# 党建工作台账应用 V3.5.0 发布说明

> **版本**：3.4.0 → 3.5.0
> **定位**：稳定性修复 + 商业化交付验收，不含新功能
> **涉及模块**：数据看板（图表渲染）、设置页 / 备份元数据（版本号）、Electron 主进程（单实例锁）、测试体系
> **数据结构**：无变化，V3.2 / V3.3 / V3.4 备份可正常恢复与融合
> **测试**：V3.5 专项 29 项 + V3.3 回归 74 项 + V3.4 回归 38 项，共 141 项全部通过

---

## 一、更新总览

| # | 模块 | 变更项 | 类型 |
|---|------|--------|------|
| 1 | 数据看板 | 5 个图表统一修复年份数据残留（notMerge），切换到无数据年份不再显示旧图 | 缺陷修复 |
| 2 | 数据看板 | 年度无数据时图表区域显示友好空态提示，替代空坐标系 | 体验优化 |
| 3 | 设置页 / 备份 | 版本号改为构建时从 package.json 自动注入，显示与元数据永不错位 | 缺陷修复 |
| 4 | 全局 | 应用单实例锁：禁止多开，重复启动自动聚焦已有窗口 | 缺陷修复 |
| 5 | 测试体系 | V3.5 专项测试 29 项 + 全量回归 112 项；新增 Windows 人工 UI 走查清单 | 质量保障 |

---

## 二、数据看板变更详情

### 2.1 图表数据残留修复（功能 1）

- **现象**：从有数据年份切换到无数据年份时，图表仍残留上一年份的渲染结果（数据"从有到无"时旧系列不会被清除）
- **修复**：看板全部 5 个图表（会议类型饼图、月度会议趋势、出勤率排行、部门出勤对比、月度谈心谈话趋势）统一启用 `notMerge` 渲染——每次以全新配置替换，不再与旧状态合并，旧系列必然清除
- 举一反三排查：5 个图表存在同类隐患，全部统一修复，不留"只修一处"死角

### 2.2 图表空态占位（功能 2）

年度无数据时不渲染空坐标系，改为等高占位提示（居中灰字 + 图表底色）：

| 图表 | 空态文案 |
|------|----------|
| 会议类型饼图 | 该年度暂无会议记录 |
| 月度会议趋势 | 该年度暂无会议记录 |
| 出勤率排行 | 该年度暂无出勤记录 |
| 部门出勤对比 | 该年度暂无会议记录 |
| 月度谈心谈话趋势 | 该年度暂无谈心谈话记录 |

### 2.3 统计逻辑纯函数化

月度趋势 series 构造逻辑提取为纯函数 `buildMonthStackSeries`（[chartSeries.ts](src/utils/chartSeries.ts)），"空白年份 → 空数组"这一根因场景纳入自动化测试。

---

## 三、版本号单一来源（功能 3）

- **现象**：设置页底部版本号停留在 v3.1，三个版本未更新；备份文件头 appVersion 依赖手工维护，存在滞后风险
- **修复**：构建时由 Vite `define` 从 package.json 注入 `__APP_VERSION__` 常量，**升版本只改 package.json 一处**：
  - 设置页底部显示（如 v3.5.0）
  - 手动备份 / 自动备份 / Excel 备份文件元数据的 appVersion 全部自动跟随
- 已构建产物经校验：版本号注入正确，无占位符残留

---

## 四、单实例锁（功能 4）

- **现象**：双击 exe 可同时运行多个实例
- **风险**：多实例并发读写同一 IndexedDB 会引发数据库锁冲突、写入丢失甚至库文件损坏；自动备份调度器多实例并发触发，产生重复/交叉备份文件
- **修复**：
  - 第二实例启动时**静默退出**（不弹框打扰）
  - 已开实例收到再次启动请求时自动**聚焦主窗口**（最小化则先还原）
- 边界说明：绿色版与安装版 userData 目录天然隔离（V3.0 设计），互不影响；本修复仅拦"同一 userData 的多开"

---

## 五、测试体系（功能 5）

- **自动化回归 141 项全部通过**：V3.3 专项 74 项 + V3.4 专项 38 项 + V3.5 专项 29 项（图表系列构造、版本号注入、单实例锁代码结构等）
- **UI 渲染层**：ECharts / antd 交互无法在 Linux 环境自动化覆盖，配套《[V3.5 人工UI走查清单](V3.5_人工UI走查清单.md)》（32 条用例，覆盖看板图表、版本号、单实例锁、人员管理、会议管理等），供 Windows 环境交付验收使用
- **打包链路**：NSIS 安装版 + 绿色版由 GitHub Actions Windows 云端构建，构建过程可追溯

---

## 六、兼容性与升级

- **数据结构零变更**：无需迁移，旧备份直接恢复
- **升级安装**：安装版直接覆盖安装；从绿色版切换安装版（或反向）请先用"备份导出"再导入
- **版本验证**：升级后设置页底部应显示 v3.5.0

---

## 七、下载

| 文件 | 说明 |
|------|------|
| `dangjian-taizhang_Setup_3.5.0.exe` | 安装版（NSIS，x64） |
| `dangjian-taizhang_Portable_3.5.0.exe` | 绿色版（Portable，x64，数据存 exe 同级目录） |

> 下载地址：[GitHub Release v3.5.0](https://github.com/Steven1233/taizhang_exe/releases/tag/v3.5.0)
> 安装版与绿色版数据目录互不相通，跨版本切换请先用"备份导出"再导入。
