# 党建工作台账应用 V3.3 更新说明（待确认稿）

> **版本**：3.2.0 → 3.3.0
> **文档性质**：需求确认与实施规划（尚未开发）
> **涉及模块**：数据看板（5 项）、会议管理（4 项）、跨模块口径统一（联调）
> **数据结构**：无 Schema 变更，无需数据迁移，备份/恢复/融合完全兼容

---

## 一、更新总览

| # | 模块 | 变更项 | 类型 |
|---|------|--------|------|
| 1 | 数据看板 | 删除"党小组会议统计"模块（页面图表 + 导出报告章节同步移除） | 功能移除 |
| 2 | 数据看板 | 导出看板报告"会议类型分布"表格删除注脚说明 | 优化 |
| 3 | 数据看板/会议管理 | 非在职人员不纳入出勤率计算（全局口径统一） | 口径修正 |
| 4 | 数据看板 | 会议总数按套会拆开计入（类型展开计次） | 口径修正 |
| 5 | 数据看板 | 平均出勤率悬停显示计算方法说明 | 交互增强 |
| 6 | 会议管理 | 参会人员编辑时体现"未选中的在职人员"，核实后录入 | 功能新增 |
| 7 | 会议管理 | 参会人员表头新增党小组筛选浏览 | 功能新增 |
| 8 | 会议管理 | 导出台账新增主题党日活动、党圈子表（组织生活会子表已存在，保留） | 功能新增 |
| 9 | 会议管理 | 导出台账参会人员名单按"参会/请假"分栏换行排版 | 优化 |

> 人员管理、谈心谈话、操作日志、数据管理四个模块**无功能性更新**，仅受第 3 项出勤口径统一影响的统计逻辑随联调修正。

---

## 二、数据看板变更详情

### 2.1 删除"党小组会议统计"模块

**V3.2 现状**：看板底部整行展示"党小组会议统计（N年）"图表（各党小组会议次数柱状 + 组内出勤率折线，双 Y 轴）；导出 Word 报告含"三、党小组会议统计"章节（党小组 / 党小组会次数 / 应到人次 / 实到人次 / 出勤率 表格）。

**V3.3 变更**：
- 页面删除该图表模块，底部空间由"出勤率排行 / 部门出勤对比 + 月度谈心谈话趋势"两列布局自然收尾
- 导出 Word 报告删除"党小组会议统计"章节，后续章节序号顺延重排（原"四、月度会议统计"→"三"，依此类推）
- `exportDashboardReport` 函数签名移除 `groupStats` 参数

**涉及文件**：
- [Dashboard.tsx](file:///workspace/src/pages/Dashboard.tsx)：删除 `partyGroupStats` 计算块、`hasGroupMeetingData`、`groupStatsOption` 图表配置、底部 JSX 模块、`getExportData` 中 `groupStats` 字段
- [exportWord.ts](file:///workspace/src/utils/exportWord.ts)：删除"三、党小组会议统计"章节（`groupHeaders` / `groupRows` / `groupTable`）及函数参数

### 2.2 导出报告"会议类型分布"删除注脚

**V3.2 现状**：类型分布表格下方有灰色小字注脚："注：党小组会次数按参与党小组数统计……各类型次数之和可能大于会议记录总数。"

**V3.3 变更**：删除该注脚段落（`typeNote`）。因第 2.4 项实施后"会议总数"本身即按展开口径统计，"各类型之和 > 记录数"的说明不再必要。

### 2.3 非在职人员不纳入出勤率计算（口径统一）

**统一口径定义**：

> 某场会议的出勤统计分母 = 该会议参会名单中、按 `isActiveAt(成员, 会议日期)` 判定为**在职**的正式成员数；分子 = 其中出席（含列席标记但状态为出席）的人数。临时人员（未匹配到人员库）及会议日期时点已非在职的成员，均不计入分子分母。

**V3.2 现状问题**：
- 看板平均出勤率（`avgRate`）：遍历全部 participants，未按会议日期校验在职状态
- 会议管理列表"出勤"列（`attended/total`）：同上
- Excel 台账"参会考勤统计"：仅列出**当前**在职人员，统计往年台账时已调离/离职人员的历史出勤数据会丢失
- Excel 台账"月度汇总"累计应到/实到：遍历全部 participants，未过滤非在职

**V3.3 变更**（全部改用上述统一口径）：

| 位置 | 变更 |
|------|------|
| 看板·平均出勤率 | participants 先按 `isActiveAt` 过滤再计算 |
| 看板·出勤率排行 | 成员每场会议先判断"该成员在该会议日期是否在职"再计入 |
| 看板·部门出勤对比 | 同上 |
| 会议管理·列表"出勤"列 | 应到/实到按在职口径计算 |
| 会议表单·智能解析冲突提示 | 沿用 `candidateMembers`（已按日期过滤），无需改动 |
| Excel·参会考勤统计 | 人员行范围扩展为"统计期内任一会议日期时在职的成员"（含现已调离但期内在职者，保证历史台账完整），每场会议按日期判断是否计入 |
| Excel·月度汇总 | 累计应到/实到按在职口径过滤 |
| Word 报告·概览/出勤排行/会议明细出勤率 | 同看板口径 |

**涉及文件**：[Dashboard.tsx](file:///workspace/src/pages/Dashboard.tsx)、[Meetings.tsx](file:///workspace/src/pages/Meetings.tsx)、[exportExcel.ts](file:///workspace/src/utils/exportExcel.ts)、[exportWord.ts](file:///workspace/src/utils/exportWord.ts)，口径工具复用 [memberStatus.ts](file:///workspace/src/utils/memberStatus.ts) 的 `isActiveAt`。

> ⚠️ **需确认**：Excel"参会考勤统计"的人员行范围是否包含"统计期内曾在职、现已调离"的人员？（建议包含，否则往年台账出勤数据不完整；若不需要，则仅列当前在职人员。）

### 2.4 会议总数按套会拆开计入

**计次规则**（沿用 V3.2 已有的 `typeMeetingUnits` 逻辑，扩展到总数指标）：

> 会议总数 = Σ 每条会议记录 Σ 每个会议类型的计次单位。其中"党小组会"= 该记录关联的党小组数（未关联时计 1 次），其他类型 = 1 次。
> 示例：一条记录类型为 [支部党员大会、党小组会（3 个党小组参加）、党课、主题党日活动] → 计 1+3+1+1 = **6 次**（用户示例中党小组会按实际参加数计）。

**V3.2 现状**：`totalMeetings = yearMeetings.length`（记录条数）；"会议类型分布"饼图与"月度会议趋势"堆叠柱状图**已**按展开口径计次，但顶部"会议总数"卡片与"本月会议数"卡片仍是记录条数，导致总数 ≠ 各类型之和。

**V3.3 变更**：
- "会议总数（N年）"卡片：改为套会拆开总次数
- "本月会议数"卡片：同口径（该月各记录各类型展开计次之和）
- 导出 Word 报告"一、年度概览"的会议总数 / 本月会议、"六、会议记录明细"引言"共召开会议 N 次"：同步新口径
- 自此：**会议总数 = 会议类型分布各类型之和 = 月度趋势各月之和**，三项指标完全自洽

**涉及文件**：[Dashboard.tsx](file:///workspace/src/pages/Dashboard.tsx)（`totalMeetings` / `monthMeetings` 计算改用 `typeMeetingUnits` 求和）、[exportWord.ts](file:///workspace/src/utils/exportWord.ts)（沿用传入的 stats，无需单独改逻辑，但明细引言行需同步）

### 2.5 平均出勤率悬停显示计算方法

**V3.3 变更**：顶部"平均出勤率"卡片标签旁增加 `<Tooltip>` 帮助图标（与现有"部门出勤对比"卡片样式一致，`QuestionCircleOutlined`），悬停内容：

```
计算方法：
平均出勤率 = 全年所有会议的在职出席人次 / 全年所有会议的在职应到人次 × 100%
· 仅统计会议日期当天处于在职状态的正式成员
· 列席人员计入出席
· 临时人员（未录入人员库）与非在职人员不计入
```

**涉及文件**：[Dashboard.tsx](file:///workspace/src/pages/Dashboard.tsx)（指标卡片 JSX）

---

## 三、会议管理变更详情

### 3.1 参会人员编辑时体现"未选中的在职人员"

**需求**：根据会议类型，将**在职但未添加到参会名单**的人员明显体现出来，供录入人核实是否存在遗漏。

**V3.3 设计**：
- 参会人员表格下方新增**"未添加的在职人员"提示区**（可折叠 Alert，默认展开）：
  - 显示剩余未添加的在职人员姓名 Tag 列表（含部室/党小组信息），点击单个 Tag 可直接添加
  - 提供"全部添加"快捷按钮（相当于将该范围人员一键补充进名单）
  - 无遗漏时显示"在职人员已全部覆盖"绿色提示
- **提示范围按会议类型自动限定**：

| 会议类型 | 未添加在职人员统计范围 |
|---------|----------------------|
| 党小组会（已选党小组） | 仅所选党小组的在职成员 |
| 党小组会（未选党小组） | 全部在职成员 |
| 其他类型 | 全部在职成员 |

- 范围基线沿用现有 `candidateMembers`（按所选会议日期 `isActiveAt` 过滤的在职人员）
- **提交拦截**：点击确定时若仍有未添加的在职人员，弹出二次确认框（"仍有 N 名在职人员未添加到参会名单：XXX、XXX……，是否确认提交？"），选择"继续提交"或"返回核实"，确保核实后录入

**涉及文件**：[MeetingForm.tsx](file:///workspace/src/components/MeetingForm.tsx)

### 3.2 参会人员表头新增党小组筛选

**V3.3 设计**：
- 参会人员表格工具栏（搜索框旁）新增**党小组下拉选择**（选项来自人员库实际存在的党小组，按 `sortPartyGroups` 排序）
- 选中某党小组后，表格仅显示该党小组成员的参会行，便于按组浏览、核对与批量调整状态
- 清空筛选恢复显示全部参会人员

**涉及文件**：[MeetingForm.tsx](file:///workspace/src/components/MeetingForm.tsx)（participants 表格 dataSource 增加党小组过滤）

### 3.3 导出台账新增子表

**V3.2 现状**：台账子表为——会议记录明细（主表）、支委会、党员大会、各党小组会（动态）、组织生活会、民主生活会 + 参会考勤统计、月度汇总、会议类型统计。

**V3.3 变更**（新增 2 个子表，组织生活会子表 V3.2 已存在、保留不动）：

| 新增子表 | 筛选条件 | 特殊字段 |
|---------|---------|---------|
| 主题党日活动 | `type` 含"主题党日活动" | 表头"会议议题"列改为 **"活动内容"**（其余列与主表一致） |
| 党课 | `type` 含"党课" | 结构与主表完全一致 |

- 子表顺序调整为：会议记录明细 → 支委会 → 党员大会 → 各党小组会 → 党课 → 组织生活会 → 民主生活会 → 主题党日活动 → 参会考勤统计 → 月度汇总 → 会议类型统计
- 实现上抽取"字段标题可定制"的明细表构建参数（`buildDetailSheet` 增加 headers 覆盖能力），避免复制整表逻辑

**涉及文件**：[exportExcel.ts](file:///workspace/src/utils/exportExcel.ts)

### 3.4 参会人员名单分栏换行排版

**V3.2 现状**：名单单元格为单行顿号拼接：`张三、李四(列席)、王五(请假)[事假]、赵六(缺席)`，长名单换行位置随意、不利阅读。

**V3.3 变更**：单元格内按状态分块、块间换行（`\n`，配合已有 wrapText 样式自动换行显示）：

```
参会：张三、李四、王五(列席)
请假：赵六(事假)、孙七(病假)
缺席：周九
```

- "参会"= 出席 + 列席（列席者标注"(列席)"），"请假"含请假原因，"缺席"行仅在存在缺席人员时显示
- 各块内仍按姓名顿号连接；某状态无人时该行整体省略
- 明细主表与所有子表同步生效

**涉及文件**：[exportExcel.ts](file:///workspace/src/utils/exportExcel.ts)（`buildMeetingRow` 的 `attendeeList` 构建）

---

## 四、跨模块口径统一（联调项）

以下为"其他无更新模块需要联调的对应修改"，保证全应用统计口径一致：

| # | 联调项 | 说明 |
|---|--------|------|
| 1 | Excel·会议类型统计表 | V3.2 中每条记录每类型 +1，与看板"套会拆开"口径不一致。V3.3 统一为按 `typeMeetingUnits` 展开计次，使台账类型统计合计 = 看板会议总数 |
| 2 | Excel·明细表应到/实到/出勤率 | 按 2.3 在职口径过滤（非在职参与者不计入四项人数与出勤率） |
| 3 | Excel·各子表合计行 | 同步 1、2 口径 |
| 4 | Word 报告·会议明细表出勤率列 | 按 2.3 在职口径计算 |
| 5 | 会议详情弹窗 | 仅展示，不改（参会名单完整显示全部人员及状态标记） |
| 6 | 数据看板/台账导出日志 | 无变化（沿用 `EXPORT_LEDGER` / `EXPORT_DASHBOARD` 日志类型） |

---

## 五、技术实现要点汇总

1. **计次口径复用**：`typeMeetingUnits(m, type)`（[Dashboard.tsx](file:///workspace/src/pages/Dashboard.tsx#L11-L17) 已有）抽离至 [types/index.ts](file:///workspace/src/types/index.ts) 或独立工具，供 Dashboard、exportExcel、exportWord 三处共用
2. **在职口径复用**：统一封装 `countActiveAttendance(meeting, members)` 工具（返回应到/实到/请假/缺席四值，按 `isActiveAt` 过滤），看板、会议列表、Excel、Word 四处调用，杜绝口径分叉
3. **Word 报告签名变更**：`exportDashboardReport(year, stats, typeStats, monthStats, rankingData, meetings, members)`（移除 `groupStats`），章节重排为一~五
4. **Excel 明细表参数化**：`buildDetailSheet` 支持 `headerOverrides`（主题党日活动子表"会议议题"→"活动内容"）
5. **表单未选人员计算**：`useMemo` 派生 `unselectedMembers`（candidateMembers − participants，党小组会时按所选党小组过滤），驱动提示区与提交拦截
6. **版本号**：[package.json](file:///workspace/package.json) `version` → `3.3.0`；`schemaVersion` 保持 3，无迁移

---

## 六、兼容性与数据影响

| 方面 | 影响 |
|------|------|
| IndexedDB Schema | 无变更（仍为 Version 3），老数据直接使用 |
| 备份/恢复/融合 | 完全兼容（数据结构未动） |
| 历史数据呈现 | 出勤率相关统计数值会因口径修正而**变化**（剔除非在职人员后，出勤率普遍小幅上升）；会议总数会因套会拆开而**变大** |
| 导出文件 | Word 报告章节减少一节；Excel 台账 Sheet 增加两个（主题党日活动、党课） |
| localStorage | 无新增键 |

---

## 七、验收清单

- [ ] 看板页面及导出报告中均无"党小组会议统计"
- [ ] 导出报告"会议类型分布"表格下方无注脚文字
- [ ] 构造"调离人员仍留在某会议参会名单"的数据，看板平均出勤率、会议列表出勤列、Excel 考勤统计均不将其计入
- [ ] 一条含 [支部党员大会、党小组会（3 组）、党课、主题党日活动] 的记录，会议总数计 6 次；类型分布各项之和 = 会议总数；月度趋势之和 = 会议总数
- [ ] 悬停看板"平均出勤率"卡片出现计算方法说明
- [ ] 会议表单中未添加在职人员有醒目提示，可单个/全部添加；带未添加人员提交时出现二次确认
- [ ] 参会人员表头党小组筛选可用，筛选后仅显示该组成员
- [ ] 导出台账出现"主题党日活动"（议题列名为"活动内容"）、"党课"子表；组织生活会子表正常
- [ ] 台账参会名单单元格呈"参会：…/请假：…/缺席：…"分块换行
- [ ] 台账"会议类型统计"合计 = 看板会议总数

---

*本文档为 V3.3 开发前确认稿，确认后将按上述范围实施。*
