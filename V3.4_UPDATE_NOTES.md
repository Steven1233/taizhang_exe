# 党建工作台账应用 V3.4.0 发布说明

> **版本**：3.3.0 → 3.4.0
> **涉及模块**：数据管理（自动备份/融合）、人员管理（变更历史/导入）、会议管理（人员提示/部门快照）、数据看板（统计口径）
> **数据结构**：新增可选字段（changeHistory / departmentSnapshot / titleSnapshot），旧数据完全兼容，V3.2/V3.3 备份可正常恢复与融合
> **测试**：V3.4 专项 38 项 + V3.3 回归 74 项全部通过

---

## 一、更新总览

| # | 模块 | 变更项 | 类型 |
|---|------|--------|------|
| 1 | 数据管理 | 定时自动备份全部数据到指定文件夹（JSON / JSON+Excel 双格式，滚动保留） | 功能新增 |
| 2 | 人员管理 | 人员变更历史可编辑：状态记录与信息变更记录统一表格管理 | 功能新增 |
| 3 | 会议管理 | 搜索添加非该日期在职人员时明确提示原因，支持"仍添加为正式成员" | 优化 |
| 4 | 人员管理 | 批量导入模板新增"入职日期 / 当前状态 / 状态变更日期"三个可选列 | 功能新增 |
| 5 | 人员管理 | 党小组下拉支持自由输入新组名（预设 ∪ 已用组名） | 优化 |
| 6 | 会议管理 | 参会记录保存部门/部室快照，换部门不改写历史统计 | 口径修正 |
| 7 | 人员管理 | 导入与手动新增同名人员查重（跳过/更新/新建三选一） | 功能新增 |
| 8 | 数据管理 | 数据融合升级：字段级冲突处理 + 疑似重复提示 + 组长 Excel 融合导入 | 功能新增 |
| 9 | 数据管理 | Excel 备份支持整库恢复（内嵌隐藏工作表，与 JSON 恢复结果一致） | 功能新增 |
| 10 | 数据看板 | "在职人员总数"更名"在职党员总数"；出勤率排行仅显示当前在职人员 | 口径修正 |
| 11 | 全局 | 状态时点判定规则修正：早于首条状态记录时沿用首条记录状态 | 规则修正 |

---

## 二、数据管理变更详情

### 2.1 定时自动备份（功能 1）
- 设置页"数据备份"区块新增自动备份配置：**目标文件夹 / 备份频率（每30分钟、每1小时、每天8点、仅启动时）/ 保留份数（超出自动清理最旧）/ 备份格式**
- 默认路径：绿色版为 exe 同级 `backups\`，安装版为 `文档\党建工作台账备份\`，均可修改（支持 U 盘/共享盘）
- 备份文件名带时间戳（如 `自动备份_党建工作台账_20260826_080000.json`），原子写入防损坏

### 2.2 Excel 备份整库恢复（功能 9）
- "JSON + Excel"格式备份的 xlsx 文件**与 JSON 备份具备同等恢复能力**
- 实现方式：Excel 内嵌隐藏工作表 `BACKUP_JSON` 分块存储整库 JSON，人读台账（17 列会议明细）不受影响
- 恢复入口同时接受 `.json` 与 `.xlsx`；无隐藏表的 xlsx（组长手工表、台账导出文件）将提示改走数据融合入口

### 2.3 数据融合升级（功能 8）
- **字段级冲突处理（8a）**：本机与备份人员信息冲突时弹窗逐条确认——本机非空白字段默认以本机为准，本机空白字段自动补齐；支持"全部保留本机 / 全部采用备份"批量操作
- **疑似重复提示（8b）**：会议/谈心谈话按"日期+名称"检测疑似重复，预览报告列明，融合后可手动清理；状态差异不自动融合，仅提示到人员编辑弹窗核对
- **组长 Excel 融合导入（8c）**：组长无需安装应用——下载填报模板（与台账导出同 17 列，含说明与示例行）→ 手工填写 → 管理员融合入口导入 `.xlsx`
  - 姓名按本机人员库匹配，**不自动新建**；未匹配姓名逐人选择处理
  - 非法日期/会议类型/名单格式**带行号拦截**，修正后重新导入
  - 出勤四列（应到/实到/请假/缺席）与名单明细不一致时以名单为准并逐行提示

---

## 三、人员管理变更详情

### 3.1 变更历史可编辑（功能 2）
- 编辑人员弹窗新增"变更历史"表格，两类记录统一管理：
  - **状态记录**（蓝标）：行内可改日期/状态、可删除、可添加——误标"借调/调离"后删除该行即恢复对应时段考勤
  - **信息变更**（灰标）：日期、字段、旧值、新值均可编辑，可删除，可手动添加；编辑保存时自动比对留痕（党小组/部门/部室/支委职务/组长/电话等）
- 校验规则：日期不可晚于今天、状态记录间不可同日；按日期自动排序
- **保存后全部统计实时重算**，无需迁移数据

### 3.2 删除人员状态可选（功能 2c）
- 删除"保留信息"时不再固定写入"离职+今天"，弹窗中**退出状态（调离/借调/离职）与变更日期均可选**（默认离职+今天，可回填实际日期）
- 批量删除统一应用一次选择，不逐人弹窗

### 3.3 批量导入增强（功能 4 / 7）
- 模板新增三个可选列：**入职日期**（修正"初始日期=导入当天"问题）、**当前状态**（支持导入即调离/借调）、**状态变更日期**，自动构建完整状态时间线；三列留空时行为与旧版一致
- **同名查重**：导入前检测本机同名人员，逐人选择"跳过（默认）/ 用导入更新（仅覆盖非空字段，保留 id 与历史）/ 仍新建"；手动新增同样拦截

### 3.4 党小组自由输入（功能 5）
- 人员表单党小组下拉从固定 5 项扩展为"预设 ∪ 本机已用组名"，可直接输入新组名回车新增，与会议表单口径一致

---

## 四、会议管理与看板变更详情

### 4.1 非在职人员添加提示（功能 3）
- 搜索下拉对非该日期在职人员标注状态与起始日期（如"借调（2026-06-15 起）· 非该日期在职"）
- 选中后弹出警示说明默认不计考勤，提供"**仍添加为正式成员**"确认入口，不再静默创建临时人员；智能解析粘贴文本同口径

### 4.2 参会部门快照（功能 6）
- 保存会议时对每位参会人快照当时的部门/部室；看板部门出勤率与导出报表优先读快照，**人员换部门不再改写历史统计**
- 旧数据无快照自动回退当前值，看板图表标题注明"按会议时点部门统计"

### 4.3 看板口径（功能 10）
- 统计卡"在职人员总数"→"**在职党员总数**"（仅文案）
- 党员出勤率排行仅显示**当前在职**人员，状态恢复在职后自动重现；部门出勤率图与导出报表范围不变

### 4.4 状态时点规则修正（规则修正）
- 会议日期早于人员首条状态记录时，由"默认在职"改为"**沿用首条记录状态**"——导入即调离的人员，其建档前的会议不再误计在职

---

## 五、兼容性与升级

- **旧版本备份**：V3.2/V3.3 的 JSON 备份在 V3.4 可正常恢复、可正常融合
- **新增字段全部可选**（changeHistory、departmentSnapshot 等），无强制迁移；schemaVersion 迁移机制保障后续版本迭代
- **升级建议**：升级后建议在设置页开启自动备份并校准重点人员的入职日期（编辑弹窗→变更历史→修改首条记录日期）

---

## 六、下载

| 文件 | 说明 |
|------|------|
| `dangjian-taizhang_Setup_3.4.0.exe` | 安装版（NSIS，x64） |
| `dangjian-taizhang_Portable_3.4.0.exe` | 绿色版（Portable，x64，数据存 exe 同级目录） |

> 安装版与绿色版数据目录互不相通，跨版本切换请先用"备份导出"再导入。
