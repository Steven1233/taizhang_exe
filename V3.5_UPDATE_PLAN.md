# V3.5 更新方案说明（稳定性交付版）

> 依据：2026-08-26 晚间用户反馈 4 项问题 + 代码取证 + grill-me 质询（Q1–Q4 已确认：Q1 选 A、Q2 选 B、Q3 认可口径、Q4 不加水印）
> 定位：**稳定性修复 + 商业化交付验收**，不含新功能
> 原则：每处修复举一反三排查同类隐患，不留"只修一处"的死角

---

## 一、问题清单（代码取证结论）

### 1. 数据看板：年份切换月度会议趋势数据错误
- **现象**：空白年份 → 非空白年份正常；非空白年份 → 空白年份时图表残留旧年数据
- **根因**：`monthStackData` 按年度过滤掉全年无数据的类型后，空白年份得到**空数组 `[]`**；`echarts-for-react` 默认合并模式（notMerge=false）下，series 从 N 条变为 0 条时**旧系列不会被移除**，图表继续显示上一次的渲染结果
- **取证位置**：[Dashboard.tsx:77-92](../src/pages/Dashboard.tsx#L77-L92)（series 构造）、[Dashboard.tsx:479](../src/pages/Dashboard.tsx#L479)（ReactECharts 实例）
- **举一反三审计**：Dashboard 共 **5 个 ReactECharts 实例**（会议类型饼图 / 月度会议趋势 / 出勤率排行 / 部门出勤对比 / 月度谈心谈话趋势），**全部存在同类隐患**——任何图表数据从"有"变"空"（如切到无数据年份、清空记录后）都会残留旧渲染。修复必须 5 处统一，不能只修月度趋势

### 2. 数据管理模块底部版本号未随迭代更新
- **现象**：设置页底部显示"党建工作台账应用 **v3.1**"，V3.2/3.3/3.4 三次迭代均未更新
- **根因**：**硬编码字符串**（[Settings.tsx:748](../src/pages/Settings.tsx#L748)），与 package.json 版本无关联；同类问题：`backup.ts` 的 `APP_VERSION` 常量也是手工维护（V3.4 期间已发现滞后一次）
- **风险**：用户报障时版本信息失真；备份文件头中的 appVersion 可能与实际不符，影响后续兼容性判断

### 3. 应用可多开（同一应用同时运行多个实例）
- **现象**：双击 exe 可重复启动多个窗口
- **根因**：[electron/main.cjs](../electron/main.cjs) 未调用 `requestSingleInstanceLock`
- **风险（商业化交付级）**：多个实例并发读写同一 IndexedDB（LevelDB）→ **数据库锁冲突、写入丢失甚至库文件损坏**；自动备份调度器多实例并发触发，产生重复/交叉备份文件
- **取证补充**：绿色版与安装版 userData 目录天然隔离（V3.0 设计），互相不影响——本修复只拦"同一 userData 的多开"，属合理边界

### 4. 全量功能测试（商业化交付标准）
- **现状**：自动化测试仅覆盖逻辑层（V3.3 74 项 + V3.4 38 项 = 112 项，Node 直跑），**UI 渲染层（ECharts / antd 交互）零覆盖**
- **沙箱限制**：本环境为 Linux，无法运行 Windows UI 实测；ECharts 渲染层无法用 Node 脚本覆盖
- **确认口径（Q3）**：逻辑层全自动化回归 + 打包链路验证 + 人工 UI 走查清单（Windows 上执行）

---

## 二、更新方案（3 项修复 + 1 项测试体系）

### 功能 1：ECharts 图表残留修复（5 处统一 + 空态占位）

**修复方式（双管齐下）：**

1. **根治残留**：Dashboard 全部 5 个 `ReactECharts` 统一加 `notMerge={true}`——每次渲染用全新 option 替换，不再与旧状态合并，从"有数据"切到"空数据"时旧系列必然被清除
2. **空态占位（Q2 选 B）**：年度无数据时图表中央显示"该年度暂无XX记录"占位文案，5 个图表统一处理：
   - 会议类型饼图：`该年度暂无会议记录`
   - 月度会议趋势：`该年度暂无会议记录`
   - 出勤率排行：`该年度暂无出勤记录`
   - 部门出勤对比：`该年度暂无会议记录`
   - 月度谈心谈话趋势：`该年度暂无谈心谈话记录`
   - 实现：无数据时不渲染 ReactECharts，改渲染等高占位 div（居中灰字 + 图表底色），避免 ECharts 空 option 的丑陋空坐标系
3. **series 构造纯函数化**：`monthStackData` 构造逻辑提取为可测纯函数（输入 meetings + year，输出 series 数组），纳入自动化测试，验证"空白年份 → 空数组"这一根因场景
4. **全局排查**： Meetings/Talks/Members 页面无 ECharts 实例（已核实仅 Dashboard 使用），无同类问题

改动：仅 `Dashboard.tsx` + 测试脚本，约 40 行

### 功能 2：版本号单一来源（构建时注入，永绝后患）

**修复方式：**
1. `vite.config.ts` 增加 `define: { __APP_VERSION__: JSON.stringify(pkg.version) }`——构建时从 package.json 读取，**升版本只改 package.json 一处**
2. `Settings.tsx` 底部硬编码 `v3.1` 改为 `v{__APP_VERSION__}`
3. `backup.ts` 的 `APP_VERSION` 常量改为引用同一注入值（手动导出、自动备份、Excel 备份的 appVersion 全部自动跟随）
4. TS 类型声明补 `__APP_VERSION__` 全局类型（`src/vite-env.d.ts` 或同位置）

改动：3 个文件约 15 行

### 功能 3：单实例锁（Q1 选 A：静默聚焦）

**修复方式（Windows 常规体验）：**
```js
const gotTheLock = app.requestSingleInstanceLock();
if (!gotTheLock) {
  app.quit();          // 第二实例静默退出
} else {
  app.on('second-instance', () => {
    // 已开实例：聚焦主窗口（最小化则还原）
    if (win) {
      if (win.isMinimized()) win.restore();
      win.focus();
    }
  });
}
```
- 不弹框、不打扰——用户双击第二次 exe 的意图就是"把应用调出来"，直接聚焦已开窗口
- 绿色版不同文件夹 userData 隔离，互不拦截（合理行为，保持现状）
- 自动备份调度器随单实例锁天然去重

改动：仅 `electron/main.cjs`，约 20 行

### 功能 4：商业化交付测试体系（Q3 口径三件套）

**① 逻辑层全自动化回归（Node 直跑，CI 可重复）**
- 现有 112 项（V3.3 74 + V3.4 38）全部保留通过
- 新增 V3.5 专项测试（预计 12–15 项）：
  - series 纯函数：空白年份 → 空数组；有数据年份 → 各类型正确计次；套会拆分计数正确
  - 版本号注入：构建产物中 `__APP_VERSION__` 替换为 3.5.0；backup 导出的 appVersion = 3.5.0
  - 单实例锁逻辑：main.cjs 锁分支代码结构静态校验（沙箱无法起 Electron，走构建产物包含性验证）
- 回归范围：状态时点判定、导入查重、融合冲突、Excel 备份往返、组长表解析（V3.4 全链路）

**② 打包链路验证**
- tsc 零错误 → vite 生产构建成功 → electron-builder 出包（NSIS + Portable 双包）
- 产物体积健全性核对（与 V3.4 基线对比，无异常膨胀）

**③ 人工 UI 走查清单（Windows 上执行，约 30 条）**
- 交付文档形式，覆盖：看板 5 图表跨年份切换（含空白年份占位）、人员增删改/变更历史编辑、会议录入全交互（搜索/智能解析/非在职提示/复用记录）、导入三选一、备份恢复（JSON/xlsx）、融合冲突弹窗、组长 Excel 导入、谈心谈话、操作日志、设置页全部开关
- 每条含操作步骤 + 预期结果 + 勾选栏，走查通过即达交付标准

改动：测试脚本 + 走查清单文档

---

## 三、明确不做

| 项 | 理由 |
|---|---|
| Playwright UI 自动化 | 单机桌面应用性价比极低，维护成本高；人工走查清单已覆盖（Q3 确认） |
| Word/Excel 报表版本号水印 | Q4 确认不做 |
| 新增任何功能 | V3.5 定位稳定性交付版 |
| 党小组更名替换工具 | V3.4 已确认暂不纳入，维持 |

---

## 四、版本与兼容性

- **版本号**：3.4.0 → **3.5.0**（package.json / Settings 底部 / 备份 appVersion 三处同源自动一致）
- **数据结构**：零 Schema 变更，V3.2/V3.3/V3.4 备份可正常恢复与融合
- **升级路径**：直接覆盖安装或替换绿色版 exe，数据无感保留

---

## 五、测试场景（V3.5 专项）

| # | 场景 | 预期 |
|---|---|---|
| 1 | 有数据年份 → 切空白年份 → 看月度会议趋势 | 图表清空 + 显示"该年度暂无会议记录"占位，无旧数据残留 |
| 2 | 空白年份 → 切有数据年份 | 各月柱状正常显示（回归确认原正常路径不破坏） |
| 3 | 5 个图表逐一重复场景 1/2 | 全部无残留（含出勤排行/部门对比/谈话趋势） |
| 4 | series 纯函数：空年份数据输入 | 返回空数组 |
| 5 | series 纯函数：套会（1 记录 3 党小组） | 党小组会计次 = 3 |
| 6 | 构建产物 grep `__APP_VERSION__` | 已替换为 3.5.0，无残留占位符 |
| 7 | Settings 底部版本号 | 显示 v3.5.0 |
| 8 | 手动/自动/Excel 三种备份的 appVersion | 均为 3.5.0 |
| 9 | main.cjs 含 requestSingleInstanceLock + second-instance 聚焦逻辑 | 构建产物包含性校验通过 |
| 10 | 双开 exe（Windows 人工） | 第二次启动不出现新窗口，已开窗口被聚焦/还原 |
| 11 | 现有 112 项回归 | 全部通过 |
| 12 | 打包双包生成 + 体积核对 | 成功且与 V3.4 基线相当 |

---

## 六、需求对照表

| 用户原始需求 | 方案覆盖 |
|---|---|
| 1. 月度趋势年份切换数据错误 + 其他地方举一反三 | 功能 1（根因修复 + 5 图表统一 + 纯函数入测） |
| 2. 数据管理版本号未随迭代更新 | 功能 2（单一来源注入，永绝后患） |
| 3. 避免同一应用多开 | 功能 3（单实例锁 + 聚焦） |
| 4. 测试所有功能达商业化交付标准 | 功能 4（自动化回归 + 打包验证 + 人工走查清单三件套） |

---

**确认本方案后实施顺序**：功能 1/2/3 并行开发 → V3.5 专项测试 + 112 项回归 → 打包双包 → 生成走查清单 → 上传 GitHub（合并 main + tag v3.5.0 + Release 附安装包，保留历史版本）。
