# V3.5.3 更新方案说明（数据看板「组织架构」模块）

> 依据：2026-09-13 用户需求 1 项（数据看板新增组织架构树状图模块）
> 定位：**纯展示型新模块**——只读呈现人员管理现有数据，不改动任何既有统计口径与功能
> 原则：数据驱动 + 零迁移——完全复用人员管理既有字段，不新增数据字段、无数据兼容问题
> 配套原型：[V3.5.3_组织架构原型.html](computer:///workspace/V3.5.3_组织架构原型.html)（可交互，请一并确认视觉与口径）

---

## 一、需求与数据现状

**需求**：数据看板下方新增「组织架构」模块——树状图自上而下按人员管理信息实时更新当前组织架构：**书记 → 副书记 → 支委（组织委员 / 宣传委员 / 青年委员等）→ 党小组**；党小组可展开显示组员姓名及所在部室，组长特殊标记，党小组旁显示人数统计；各层级均显示当前任职人员姓名。

**数据现状（字段已齐备，无需任何数据变更）：**

| 字段 | 来源 | 模块用途 |
| --- | --- | --- |
| `committeeRole` | 人员管理「支委职务」（COMMITTEE_ROLES 九种单选） | 书记 / 副书记 / 支委层级归属 |
| `partyGroup` | 人员管理「党小组」 | 党小组层级分组与人数统计 |
| `isGroupLeader` | 人员管理「党小组组长」勾选 | 组长 ★ 标记 + 红色「组长」标签 + 组内置顶 |
| `title` | 人员管理「部室」 | 组员所在部室展示 |
| `status` + `statusHistory` | 人员管理「状态」及状态历史 | 在职 / 借调 / 调离 / 离职时间线判定 |

**渲染技术选型：自绘 HTML/CSS 树（原型即此方案）**

| 方案 | 结论 |
| --- | --- |
| ECharts tree | 节点需「职务 + 姓名 + 多标签 + 部室」富样式与按组独立展开交互，ECharts tree 的 label 富文本与交互控制弱；看板已有 4 图，DOM 树更轻量 |
| antd Tree | 缩进式目录树，非组织架构自上而下形态，视觉不符 |
| **自绘 HTML/CSS**（选定） | 样式 / 交互完全可控，与 antd 卡片风格一致，连接线纯 CSS，**零新增依赖** |

---

## 二、模块设计

### 1. 树结构（自上而下四层）

```
        [支部书记] 王建国
              │
        [支部副书记] 李明
              │
   ┌─────────┼─────────┐
 组织委员   宣传委员   青年委员 ……      ← 支委会（仅显示有在职人员的职务，按职务定义序）
  张 丽     刘 洋     陈 晓
              │
   ┌─────────┼─────────┬─────────┐
 第一党小组  第二党小组  第三党小组  未编组   ← 党小组（可展开；卡片旁显示人数）
  （8人）    （7人）    （6人）   （2人）
```

### 2. 数据口径（严谨性核心）

| 口径项 | 规则 |
| --- | --- |
| 人员范围 | 当前时点状态为**在职或借调**的人员（新增 `statusAt` 时间线函数判定，回溯补录的变更同样正确反映）；**调离 / 离职不显示** |
| 借调人员 | 组织关系仍在支部——保留展示，带橙色「借调」标签 |
| 书记 | `committeeRole === '支部书记'`；节点常显，无在职人员时显示「空缺」灰字（保证架构顶层稳定） |
| 副书记 | `committeeRole === '支部副书记'`；无人员时**不显示该节点**（支部可不设副书记） |
| 支委 | 其余职务按 COMMITTEE_ROLES 定义序（组织 → 宣传 → 青年 → 纪检 → 保密 → 统战 → 群工）；**仅显示有人员的职务**；同一职务多人全部列出 |
| 党小组 | 按 `partyGroup` 分组、`sortPartyGroups` 排序（「第N党小组」按序号优先）；人数 = 组内当前在职 + 借调人数（不含调离 / 离职） |
| 组长 | `isGroupLeader === true`：**组内置顶** + ★ + 红色「组长」标签（一组多组长属数据异常，全部标记） |
| 组员 | 姓名 + 部室（title）；有支委职务者附带蓝色职务标签；借调者附带橙色「借调」标签 |
| 未编组 | `partyGroup` 为空的人员归入「未编组」虚线节点（有人员才显示），人数照常统计 |
| 实时更新 | 进入数据看板时从数据库读取最新人员数据（现有 loadData 机制），人员管理任何变动后进入看板即为最新 |
| 年份关系 | **不随看板年份切换**——始终展示当前时点架构（历史架构回溯不在本期范围） |
| 空态 | 无任何在职 / 借调人员 → 模块显示占位提示 |

### 3. 交互与视觉（见原型）

- 党小组卡片点击头部展开 / 收起组员名单（默认全部收起）；卡片右上角「展开全部 / 收起全部」批量操作
- 卡片头部：组名 + 人数徽标（红色）；展开后组员单列排布
- 连接线浅红色，书记 / 副书记节点红框重点样式，与看板既有红色主题（#CC0000）一致
- 卡片副标题显示汇总：共 N 人 · 在职 x · 借调 y；标题旁 Tooltip 说明口径
- 模块位置：数据看板最下方（月度谈心谈话趋势之后），通栏 Card

### 4. 实现要点

1. 新增纯函数 `statusAt(member, date): MemberStatus`（[memberStatus.ts](src/utils/memberStatus.ts)）——`isActiveAt` 的时间线版，但返回具体状态；**不改动 `isActiveAt`**（避免影响既有统计），测试断言两者等价性
2. 新增纯函数 `buildOrgChart(members, refDate): OrgChartData`（新文件 [src/utils/orgChart.ts](src/utils/orgChart.ts)）——产出树结构数据（书记 / 副书记 / 支委 / 党小组 / 未编组 / 汇总），组件内零计算逻辑
3. 新增组件 `src/components/OrgChart.tsx`——antd Card + 自绘树 + 展开状态管理（React state）；Dashboard 底部引入（约 5 行）
4. Word 看板报告不包含组织架构（纯看板展示模块，不影响既有导出）

---

## 三、涉及文件清单

| 文件 | 改动 | 规模 |
| --- | --- | --- |
| [src/utils/memberStatus.ts](src/utils/memberStatus.ts) | 新增 `statusAt` 纯函数 | ~20 行 |
| src/utils/orgChart.ts | 新增 `buildOrgChart` 纯函数 + 类型 | ~90 行 |
| src/components/OrgChart.tsx | 新增组件（含样式） | ~220 行 |
| [src/pages/Dashboard.tsx](src/pages/Dashboard.tsx) | 底部引入组织架构模块 | ~5 行 |
| [scripts/v35-test.ts](scripts/v35-test.ts) | 追加 V3.5.3 专项测试 | ~100 行 |
| package.json | 版本号 3.5.3 | 1 行 |

## 四、测试方案

**自动化（v35-test.ts 追加约 14 项，Node 直跑，沿用 check 框架）：**

- `statusAt`：时间线判定（无历史兜底 / 借调期间 / 借调回归 / 早于首条沿用首条状态）；与 `isActiveAt` 等价性断言
- `buildOrgChart`：书记 / 副书记分层；支委按定义序排列；无人员职务不显示；多人同职务全列；调离 / 离职排除；借调含入且带标记；组长置顶与人数统计；未编组归集；组员部室 / 职务标签数据；空数据空态

**人工走查（Windows，约 5 项）：**

1. 导入测试数据备份 → 看板底部出现组织架构，书记 / 支委 / 党小组与人员管理一致
2. 人员管理调整某组组长 / 新增人员 / 改状态 → 回到看板架构图即时反映
3. 点击党小组卡片展开 / 收起；「展开全部 / 收起全部」正常
4. 将某人状态改为调离 → 从架构图消失；改为借调 → 带「借调」标签保留
5. 清空在职人员 → 空态占位正常

## 五、兼容性与发布

- **零数据变更、零口径变更**——不触碰既有统计与导出，回归风险极低
- **发布**：v3.5.3，流程同 v3.5.2（本地 portable 验证 + tag 触发 GitHub Actions 双包构建 + Release 发布）

## 六、待确认项（原型一并确认）

| # | 决策点 | 推荐方案 | 备选 |
| --- | --- | --- | --- |
| 1 | 借调人员 | 展示 + 橙色「借调」标签（组织关系仍在支部） | 仅显示在职（借调隐藏） |
| 2 | 年份关系 | 不随年份切换（始终展示当前架构） | 按所选年份展示历史架构（复杂度高，不建议） |
| 3 | 副书记空缺 | 隐藏节点 | 显示「空缺」（同书记处理） |
| 4 | 未编组人员 | 归入「未编组」虚线节点展示 | 不显示 |
| 5 | 党小组默认态 | 默认收起 | 默认全部展开 |
