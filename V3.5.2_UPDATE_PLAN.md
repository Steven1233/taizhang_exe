# V3.5.2 更新方案说明（数据看板口径修正版）

> 依据：2026-09-13 用户反馈 2 项数据看板需求 + 代码取证
> 定位：**数据看板统计口径修正 + 出勤对比维度扩展**，小版本迭代
> 原则：计算严谨性优先——所有统计口径有明确定义、可验证、与既有口径（V3.3 时间线在职 / V3.4 快照机制）保持一致

---

## 一、问题清单（代码取证结论）

### 1. 数据看板：「在职党员总数」不随年份切换更新

- **现象**：切换年份后，会议总数 / 平均出勤率 / 本月会议数均按所选年份变化，唯独「在职党员总数」始终显示同一数字
- **根因**：[Dashboard.tsx:69](../src/pages/Dashboard.tsx#L69) `members.filter((m) => m.status === 'active')`——按人员**当前状态**过滤，与所选年份完全无关。人员状态历史（statusHistory，V3.0 引入）已完整记录在职/调离/借调/离职的时间线，但该指标未使用
- **连带问题**：Word 看板报告中的「在职党员」指标（[exportWord.ts:160-168](../src/utils/exportWord.ts#L160-L168)）同样使用当前状态口径，需同步修正

### 2. 出勤对比仅支持「部门/支部」单一维度

- **现状**：[Dashboard.tsx:139-164](../src/pages/Dashboard.tsx#L139-L164)「部门出勤对比」固定按部门/支部（department）统计
- **需求**：默认按**党小组**统计，同时支持切换「部室」（title）、「部门/支部」（department）
- **严谨性缺口**：V3.4 功能 6 为参会记录建立了部门/部室快照（departmentSnapshot / titleSnapshot，"人员换部门不改写历史"），但**党小组无快照机制**——党员年中调动党小组后，历史会议的党小组归属会被改写，直接扩展维度会出现口径失真

---

## 二、更新方案（2 项功能）

### 功能 1：「在职党员总数」按所选年份动态统计

**口径定义（年报口径，推荐）：**

> 在职党员总数 = 截至「参考时点」状态为在职的党员人数
> - **参考时点**：所选年份为过去/未来年份 → 该年 **12 月 31 日**；所选年份为当前年份 → **当天**（截至目前）
> - **入库时点判定**：首条状态记录日期（无状态历史时取记录创建日期）不晚于参考时点——**该年份尚未入职/组织关系未转入的人员不计入**（如 2025 年 1 月入职者不计入 2024 年末数）
> - **状态追溯**：复用 `isActiveAt` 状态历史时间线判定（借调期间不计入、借调回归后恢复、调离/离职后不计入）
> - **兜底**：无状态历史的旧数据按当前状态判定（与 `isActiveAt` 现有兜底一致）
> - **借调口径维持现状**：借调期间不计入在职（与现有指标、考勤口径一致；如需将借调人员计入，请确认时说明）

**实现要点：**

1. 新增纯函数 `countActiveMembersAt(members, refDate)` 到 [memberStatus.ts](../src/utils/memberStatus.ts)——"入库时点 + isActiveAt" 双重判定，**不改动 `isActiveAt` 本身**（避免影响考勤统计的既有行为）
2. Dashboard KPI 卡片改用该函数，参考时点按上述规则取值
3. 卡片标签带时点：过去/未来年份显示「在职党员总数（{year}年末）」，当前年份显示「在职党员总数（截至目前）」；新增 Tooltip 说明口径
4. Word 报告「在职党员」指标同步改为年度口径，标签同步
5. 空年份（无任何数据）同样按人员状态时间线计算——该指标不依赖会议数据

**验证基准（基于测试数据备份，46 人名单）：**

| 年份 | 参考时点 | 预期值 | 排除人员 |
| --- | --- | --- | --- |
| 2024 | 2024-12-31 | **41** | 顾建军、秦晓东、白雪梅（尚未入职）；蒋明辉（2024-11 调离）；孔祥明（2024-06 即调离） |
| 2025 | 2025-12-31 | **41** | 蒋明辉、孔祥明、沈玉兰（2025-06 调离）；杨春花（2025-08 离职）；白雪梅（尚未入职） |
| 2026 | 今天 | **40** | 上述 4 名调离/离职 + 韩志远（2026-01 离职）+ 冯建军（2026-01 借调） |
| 2031 | 2031-12-31 | **40** | 同 2026（无更晚状态变更，按最后已知状态） |

### 功能 2：出勤对比支持党小组（默认）/ 部室 / 部门/支部 三维度

**党小组快照机制（严谨性核心，沿用 V3.4 功能 6 完全相同的模式）：**

1. `Participant` 类型新增可选字段 `partyGroupSnapshot?: string`（保存会议时的党小组）
2. [Meetings.tsx:127-137](../src/pages/Meetings.tsx#L127-L137) 保存会议时补写党小组快照（仿现有部门/部室快照，约 2 行）
3. [MeetingForm.tsx:478-485](../src/components/MeetingForm.tsx#L478-L485) 「复用上一次记录」时同步清除党小组快照（现有逻辑只清部门/部室，需补齐，否则复用会带上旧快照）
4. **旧数据回退**：无快照的历史会议按党员**当前**党小组归属（与部门维度现有回退口径完全一致）
5. 不采用「信息变更留痕（changeHistory）回溯党小组」方案——changeHistory 定位为只读展示、不参与统计判定（V3.4 设计约定），且仅 V3.4 后自动生成、历史数据不完整，作为统计依据不可靠

**三维度统一计算口径（全部沿用既有严谨规则）：**

| 口径项 | 规则 |
| --- | --- |
| 维度取值 | 党小组：`partyGroupSnapshot ?? 当前党小组`；部室：`titleSnapshot ?? 当前部室`；部门/支部：`departmentSnapshot ?? 当前部门` |
| 时间线在职 | 每场会议按该场日期 `isActiveAt` 判定——借调/调离/离职期间不计入，离开前及回归后照常计入 |
| 行范围 | 年内任一会议日期时点在职的人员（`membersActiveDuring`） |
| 支委会排除 | 类型含「支部委员会」的会议（含套会）不纳入——支委会仅支委参加，纳入会扭曲各部门/小组基数（维持现有规则） |
| 列席 | 计入出席（维持现有规则） |
| 临时人员 | 不计入（memberId 与人员库不匹配，自然排除） |
| 空值 | 未编组（党小组为空）/ 空部室 / 空部门的人员不计入该维度（与现有部门空值处理一致） |
| 出勤率 | 该维度出席人次 ÷ 应到人次 × 100%，显示保留 1 位小数 |
| 排序 | 按出勤率**降序**（现版为插入序无排序，改为降序便于对比） |

**实现要点：**

1. 新增纯函数 `buildDimensionAttendanceRates(meetings, members, dimension)` 到 [chartSeries.ts](../src/utils/chartSeries.ts)——现有部门统计逻辑（Dashboard L139-164）泛化为三维度，输出 `{ name, attended, total, rate }[]`（降序）
2. Dashboard 卡片标题改为「出勤对比」，标题右侧加 `Segmented` 三项切换：**党小组（默认）** / 部室 / 部门/支部；维度选择在切换年份时保持不重置
3. 图表标题、Tooltip 文案随维度联动（注明快照口径、支委会排除、空值不计入）；部室维度类目较多时 x 轴标签自动倾斜
4. ECharts 已全局 `notMerge`（V3.5），维度切换时旧维度系列自动清除；无数据时维持空态占位

---

## 三、涉及文件清单

| 文件 | 改动 | 规模 |
| --- | --- | --- |
| [src/types/index.ts](../src/types/index.ts) | `Participant` 新增 `partyGroupSnapshot?: string` | ~3 行 |
| [src/pages/Meetings.tsx](../src/pages/Meetings.tsx) | 保存时补写党小组快照 | ~2 行 |
| [src/components/MeetingForm.tsx](../src/components/MeetingForm.tsx) | 复用记录时清除党小组快照 | ~2 行 |
| [src/utils/memberStatus.ts](../src/utils/memberStatus.ts) | 新增 `countActiveMembersAt` 纯函数 | ~20 行 |
| [src/utils/chartSeries.ts](../src/utils/chartSeries.ts) | 新增 `buildDimensionAttendanceRates` 纯函数（吸收 Dashboard 现有部门逻辑） | ~45 行 |
| [src/pages/Dashboard.tsx](../src/pages/Dashboard.tsx) | KPI 口径 + 标签/Tooltip；出勤对比维度切换 UI | ~60 行 |
| [src/utils/exportWord.ts](../src/utils/exportWord.ts) | 「在职党员」指标与标签同步年度口径 | ~2 行 |
| [scripts/v35-test.ts](../scripts/v35-test.ts) | 追加 V3.5.2 专项测试段（详见下） | ~120 行 |
| package.json | 版本号 3.5.2 | 1 行 |

## 四、测试方案

**自动化（scripts/v35-test.ts 追加约 18 项，Node 直跑，沿用 check 框架）：**

- 功能 1（约 8 项）：2024/2025/2026/2031 四个年份计数（对齐上表预期值 41/41/40/40）；入库前不计入；调离后不计入；借调期间不计入、回归后恢复；无状态历史兜底
- 功能 2（约 10 项）：三维度聚合正确性（合成数据精确断言）；党小组快照优先于当前值；旧数据按当前党小组回退；空值（未编组/空部室/空部门）不计入；支委会（含套会）排除；借调期间不计入；列席计出席；临时人员不计入；降序排序

**人工走查（Windows，约 6 项）：**

1. 导入测试数据备份 → 切换 2024/2025/2026/2027/2031 年份，「在职党员总数」随年份变化且带时点标签
2. 出勤对比默认显示党小组维度；切换部室、部门/支部，图表即时刷新无残留
3. 维度切换后切到无数据年份（如 2028）→ 空态占位正常
4. Tooltip 三维度文案正确
5. 导出 Word 报告 →「在职党员」数值与看板一致
6. 编辑保存一条新会议 → 检查参会记录带党小组快照（导出 JSON 备份验证）

## 五、兼容性与发布

- **数据兼容**：`partyGroupSnapshot` 为可选字段，旧备份/旧库自动兼容（undefined 时回退当前值）；对 V1/V2/V3 备份导入无影响
- **口径变化说明**：「在职党员总数」从"当前在职数"变为"年度时点在职数"——当前年份下数值与旧版一致（截至目前 = 当前状态），仅历史/未来年份呈现差异
- **发布**：v3.5.2，流程同 v3.5.1——本地构建 portable + tag 触发 GitHub Actions 构建 NSIS，双包发布

## 六、口径决策（已确认，2026-09-13）

| # | 决策点 | 确认结果 |
| --- | --- | --- |
| 1 | 「在职党员总数」时点口径 | **年末时点**（当前年份=截至目前） |
| 2 | 党小组维度旧数据回退 | **按当前党小组**（新保存会议起建快照） |
| 3 | 未编组/空部室/空部门人员 | **不计入该维度** |

另有两处口径**维持现状**：借调人员不计入在职党员数；支委会不纳入出勤对比。
